version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.2-stretch-node
    resource_class: xlarge
    steps:
      - checkout
      - run: ./scripts/test.sh
      - run: ./scripts/deploy.sh

workflows:
  version: 2
  build_and_test:
    jobs:
      - build

